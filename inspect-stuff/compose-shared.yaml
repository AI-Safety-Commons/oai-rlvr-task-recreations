# Kept as the default run target for compatibility with existing run files.
# All simulated responses persist in the gateway across episodes.
# Inspect scans this entry file for metadata names before invoking Compose:
# SAMPLE_METADATA_GATEWAY_SAMPLE_ID
# SAMPLE_METADATA_GATEWAY_TASK_CONTEXT
# SAMPLE_METADATA_GATEWAY_CONTROL_TOKEN
include:
  - compose.yaml
