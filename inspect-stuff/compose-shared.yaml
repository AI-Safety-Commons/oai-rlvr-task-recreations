# Kept as the default run target for compatibility with existing run files.
# Cross-episode wiki/paste-style state now lives in the policy gateway's
# persistent simulator rather than in separate fake-internet services.
include:
  - compose.yaml
